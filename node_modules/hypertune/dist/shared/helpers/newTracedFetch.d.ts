import { LocalLogger, TracedFetch } from "../types";
export default function newTracedFetch({ timeoutMs, localLogger, }: {
    timeoutMs: number;
    localLogger: LocalLogger;
}): TracedFetch;
//# sourceMappingURL=newTracedFetch.d.ts.map