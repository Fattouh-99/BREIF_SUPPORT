import Node from "./Node";
export declare class VoidNode extends Node {
    typeName: "Void";
    get(options?: {}): void;
}
export declare class BooleanNode extends Node {
    typeName: "Boolean";
    get({ fallback }: {
        fallback: boolean;
    }): boolean;
}
export declare class IntNode extends Node {
    typeName: "Int";
    get({ fallback }: {
        fallback: number;
    }): number;
}
export declare class FloatNode extends Node {
    typeName: "Float";
    get({ fallback }: {
        fallback: number;
    }): number;
}
export declare class StringNode extends Node {
    typeName: "String";
    get({ fallback }: {
        fallback: string;
    }): string;
}
//# sourceMappingURL=primitiveNodes.d.ts.map