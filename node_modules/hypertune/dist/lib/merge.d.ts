import { DeepPartial, Value } from "../shared/types";
/**
 * merge recursively merges sources into value, returning a value.
 */
export default function merge<T extends Value>(value: T, ...sources: DeepPartial<T>[]): T;
//# sourceMappingURL=merge.d.ts.map