import { InitData, InitDataProvider } from "../../shared/types";
export default class VercelEdgeConfigInitDataProvider implements InitDataProvider {
    private readonly edgeConfigClient;
    private readonly itemKey;
    constructor({ edgeConfigClient, itemKey, }: {
        edgeConfigClient: {
            get: (key: string) => Promise<any>;
        };
        itemKey: string;
    });
    getName(): string;
    getInitData(): Promise<InitData>;
}
//# sourceMappingURL=VercelEdgeConfigInitDataProvider.d.ts.map