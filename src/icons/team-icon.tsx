import { Users } from 'lucide-react'
import React from 'react'

type Props = {}

const TeamIcon = (props: Props) => {
  return (
    <Users className="text-indigo-900 dark:text-indigo-100" />
  )
}

export default TeamIcon 